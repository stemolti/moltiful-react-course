// EXPORT IMPORT TYPES
/* 
import first from './utils.js';
import {apiKey} from './utils.js';
import * as utils from './utils.js';
import {apikey, abc as content } from './utils.js'; 
*/

// console.log(util.apiKey);
// console.log(content);

//console.log('Hello STEY!');

/* 
function combine (a, b, c){
  return a * b / c
}

console.log(combine(6,5,5)); */


/* function transformToObjects(numbersArray){
  const editedObjArray = numbersArray.map(num => ({
    val: num
  }));
return editedObjArray; 
}

console.log(transformToObjects([1, 2, 3])); */